{
    "name": "repo-sync-action",
    "version": "1.0.0",
    "description": "A GitHub Action to synchronize files and tags across multiple repositories.",
    "main": "index.js",
    "dependencies": {
        "@actions/core": "^1.10.1",
        "@actions/exec": "^1.1.1",
        "fs-extra": "^11.2.0"
    },
    "scripts": {
        "build": "ncc build index.js --source-map --license licenses.txt",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.3"
    },
    "keywords": [
        "github",
        "actions",
        "sync",
        "repository"
    ],
    "author": ""
}